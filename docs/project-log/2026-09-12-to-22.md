# 从启动到当前：需求与实施记录

整理日期：2026-09-22。覆盖本会话可见的全部工作主题；重复“继续”、授权执行与同一问题的追问并入对应步骤。时间分组以 Git/旧文档为依据，不为未留时间戳的发言虚构具体日期。状态写的是本次核查结果，不能代替线上验收。

## 1. 启动、账户、开发与上线链路（09-12—09-13）

1. **审查同事提出的 GitHub / Netlify 工作流。** 公司资产归属 XIONS GROUP，优先使用 `contact@xionsgroup.com`；Lovable 可选；本地/Codex/Claude → GitHub dev → Netlify 分支预览 → 审核 → main → 正式站。原始要求为公司邮箱、不用私人账户，后续用户明确授权当时确认的两个账户用于创建公司项目；不能在新窗口重复要求重建账户。
2. **检查 Web 文件夹原文、缺失内容、网站布局与 Mobbin 参考。** 建立集团/品牌/承诺/媒体/招聘/联系/法律结构；旧方案保留在 `docs/archive/planning/`。当前布局以源码和 `docs/project-map.md` 为准，历史参考不是待实现页面清单。
3. **要求逐步写清本地写代码、GitHub 连接、Netlify 连接、预览及上线。** 改用 Astro 静态组件/共享模板，不采用 Shopify section 机制。操作和维护手册保留在 `docs/archive/deployment/`，当前命令在 README。
4. **询问图片存哪里、如何快速改文字、是否有后台。** 上线素材放 `public/images/`，页面/共享内容在源码；公司原件留 `local-materials/` 并忽略。无 CMS；后台编辑属于后续可选工作，不假定已接入。
5. **授权创建 GitHub 仓库与 Netlify 托管并连通 GoDaddy。** 现有仓库 `XIONS-GROUP/xionsgroup-website`；Netlify 项目名 `xionsgroup`。GoDaddy 负责域名 DNS，指向 Netlify，不指向 GitHub。仓库分支配置证据见 Git 与 `netlify.toml`；远端账户权限不由本轮重新审计。
6. **确认 DNS 已接通、本地 dev 推远端 dev 用于预览，main 为正式版本。** 保留此分支约定；但后来发生 DNS/SSL 待验证提示，不能用最初成功状态证明当前远端健康。
7. **整理本地项目。** 部署入口合并到根目录，旧代码放 `archive/`、文档归入 `docs/`、原件放 `local-materials/`；`bacbaba`、`d8f1ee6`。`main` 保留预热 Logo 页（`9b56a72`）。

## 2. 页面、设计和源文案（09-13—09-15）

8. **完整做出规划页面、相似页面共用模板，缩短冗长网站、达到奢侈品牌的编辑设计感。** 多页站和共享布局落地，`1aac28a`；经历暗色/液态实验后回到白色社论主体，早期尝试见 Git 快照。品牌页共用 `BrandPage`，法律页共用 `LegalPage`，不是每页独立复制。
9. **反复对照 `xions-group-web-text-corrected.txt`，时间紧不大改文案，仅修错字与一般问题。** 当前原件为 `local-materials/content/xions-group-web-text-corrected.txt`；不把全部原始公司材料推到公开资源目录。Pappers 来源：https://www.pappers.fr/entreprise/xions-group-912563319 。原件/公共资料核对与法律正确性审核是不同事项。
10. **导航过复杂、下拉太透明、全大写太多、去箭头/分隔阴影；品牌四卡居中、去编号、放 Logo/类别/简介。** `668afad`。后续用户明确主导航标题保留大写，其余正文/子菜单正常大小写；新需求覆盖早先“避免所有大写”的泛化理解。
11. **先做内容差距规划；保留国际化词汇；媒体与 Engagement 分开，菜单加入招聘。** 当前主导航显示 `Press`，链接 `/fr/presse/`，另有 Engagements / Carrières。会话曾口述“Price”，当前源码和后来请求明确是 Press，未擅自建立价格页面。
12. **法律公司资料不对、增加到 Contact 的链接；首页暂时纯白并保留未来图片/轮播位置。** `0e462ac`；法律/隐私/Cookies/使用条款从原文补全，`449beeb`。后来首页摄影轮播方案被程序化 X Hero 替代；不要为完成旧任务重新加轮播。
13. **修复集团/品牌悬停残留、父级无法点击、背景发黄。** `d93e95c`；只有有子项的两个菜单触发下拉，移到 Press 等直链会关闭，父项可进入总览。白色磨砂导航为明确要求；全站 body 的 `--paper` 当前仍是近白 `#fefdfb`，如需全站严格纯白另行确认，不写成已全站替换。
14. **品牌描述准确性、上线差距、视觉用途和尺寸清单；品牌红固定 `#c20000`。** `a231b84`；品牌当前内容源 `src/data/brands.ts`，网站仍有摄影和法律确认待办。
15. **给每张图明确文件名；iOS 的页脚箭头变 emoji。** `603d498`；使用 SVG `Arrow.astro` 统一箭头，不依赖系统字符渲染。图像命名/尺寸后来集中进 visual manifest。

## 3. Visual 清单与设计工具试验（09-15—09-16）

16. **询问能否控制 Affinity 批量建画板及便宜模型选择；先分析，再试三个规格。** 讨论与试验属于素材制作辅助，不是网站生产依赖；没有必要为了网页再次启动图形软件或比较模型价格。
17. **用户纠正复制画板重叠、尺寸未改；要求删除并重建。** 留下明确操作知识：画布四周加号新增不重叠画板；中键平移；右下角 W/H 改像素尺寸。不能只重命名而不改尺寸。当前仓库不能证明 Affinity 三个试验板的最终状态，不标记已验收。
18. **提议多页空白 PDF，按尺寸生成再改名；融合版 Affinity 将 PDF 导入为排版页面而非画板。** 记录为试验受阻/方案被替代，不能宣称 PDF 已可靠一键变 Affinity 画板。当前本仓库未找到可核实的该空白 PDF 成果。
19. **按相同尺寸重分 checklist；用户先收集图片，并要求从上线到维护规划。** 当前清单 `local-materials/launch-content-and-visual-checklist.md`；以前版本在 `local-materials/visual-kit/`。正式准备工作归入当前状态待办。
20. **质疑手机版 Hero 1200×1600（3:4）无法覆盖全屏，要求先确定位置、比例、UI占位。** `6d34a27`：manifest 驱动，支持 `cover/contain`，手机摄影槽修订为1440×3120（6:13），桌面2560×1440；开发版显示可感知位置的占位图/规划页。两种摄影槽目前被 X 动画替代，保留为将来可选，不能仍算上线必须缺图。
21. **手机菜单应全屏；集团不展开 Notre équipe、品牌显示四家；恢复首页活动logo和 Press原文内容；两层同磨砂；全部品牌链接移到末尾；修复“De l’idée au rayonnement”标题。** `3738703`。Press按原文组织七个活动图库和三个传播图库，首页保留六标识；具体摄影仍依素材到位情况。
22. **更新适合 Illustrator 的一键脚本，包含全部网站图片、命名和尺寸。** `f551019`；由 `scripts/generate-visual-kit.mjs` 和 `src/data/visuals.json` 生成 JSX/索引/CSV/SVG；输出在忽略目录 `local-materials/visual-kit/`。这是已选工作流，避免继续保证 Affinity 的 PDF 导入行为。
23. **讨论首页封面应如何体现集团定位。** 曾考虑品牌/文化/科技相关摄影，后续明确选黑白 X 光场；历史视觉建议见 `docs/visual-direction-2026-09-17.md`，现阶段不要继续强迫补 Hero 照片。

## 4. 图片实装、菜单与表单（09-16—09-17）

24. **桌面导航/下拉应为一整块伸展磨砂面；手机自上滑下抽屉；品牌两列扩大间距。** `d107138`；统一 `Navigation.astro` 背景与展开高度。
25. **整理 public visual；画板1—6重命名分类并预览。** 映射：1 Cannes、2 César、3 International Emmy、4 Golden Globes、5 Grammy、6 Oscars；归档 `public/images/awards/`，其他素材按品牌/集团/社交分类。
26. **活动按举办顺序、标识太小要按新比例放大；手机关闭菜单先消字再收起的延迟。** `029e7b5`；当前顺序 Golden Globes → Grammy → César → Oscars → Cannes → Emmy（全年通常先后，具体年份日期不硬编码）；关闭文字/背景同步开始。
27. **Contact“其他”需要自填标题；同意勾选是否必要；404失败且要有成功页。** `64fee53`。移除表单强制同意框，保留隐私用途说明；成功页含后续导航。法律依据仍须公司确认，不能从“删框”推断全部隐私文案已一致。
28. **进一步简化：Objet分类必选，补充标题始终可选，与分类同行，包括“其他”；仍提交失败，先查原因再执行。** `9ddcbc7`；现表单 `subject` 必填，`subject-detail` 非必填；AJAX URL-encoded POST 到 `/`，服务器成功后跳到 `form.action`。`public/__forms.html` 提供部署扫描声明，`a56d218`触发重新检测。原始错误码完整诊断不在当前记录中，不虚构为唯一已证实根因。
29. **成功页出现但没收到邮件，要求核对通知链路。** 会话后续用户提供收到通知的截图，说明至少一封技术测试到达。该邮件名称 `XIONS Website Delivery Test` 是测试内容，不是访客实际姓名；不能拿这封邮件证明真实访客字段映射和投递已全部验收。
30. **收到的通知不专业，要求清晰公司表单排版；讨论 GitHub credit、Resend验证/API Key/用途。** 本地存在邮件排版原型和可选 Resend方案；随后用户明确希望 Shopify式简单通知、不想接额外第三方发信API。最终保留 Netlify Forms 内置通知；Resend原型未启用，不能把本地HTML预览当作线上通知模板。当前 `docs/contact-email-*` 等仍是未提交旧工作；上线前需要真实字段/后台记录/收件箱三方核对，不自动发测试邮件。
31. **品牌页加官网/Instagram；Betenoir未上线；更新白底小图标与 Golden Globes；统一所有小标题对齐，评估缺图和动感。** `705229b`。官网、社交链接在 `brands.ts`；Betenoir `websiteLive:false`、Instagram为null。favicon /活动logo已接入，图库依manifest；对齐规则跨页统一。

## 5. 语言、SEO与部署排查（09-21）

32. **先规划双语与SEO：删除页脚内部 Visual Preview链接，页脚和手机菜单能快速切换，整站英文。** 用户随后授权实施，`16bff79`：法英共用模板，17路由/语言，30公开URL sitemap；完整正文/法律/表单/alt/metadata翻译，canonical、hreflang、JSON-LD和分享封面。
33. **新分享封面已放 public，要求整理。** 1200×630图在 `public/images/social/xionsgroup-og.png`，网站白底图标在 `public/images/site/`，更新资产清单；原文档记载César标识也在此次更新。
34. **用户反复问部署为什么没好、额度消耗、是否装CLI，并明确 Netlify 用插件/CLI不要 Computer Use。** 后续运维遵循该偏好；不把不必要浏览器操作当排错。已提交修复 `a710fd7`：移除会阻挡英文路由的错误404兜底规则，更新英文别名。
35. **主域及跳转：用户截图确认 www.xionsgroup.com 为 Primary；问Safari/Chrome显示是否相同；后续截图又显示 Pending DNS verification 和SSL读取失败。** 文本曾写 `www.zungufo.com`，截图和项目实际是 `www.xionsgroup.com`；以核对的真实域名为准。浏览器地址栏隐藏www不等于未跳转，应检查实际URL/HTTP链。前后截图矛盾说明需重新验证，不能把旧 `PROJECT_STATUS` 的“DNS/TLS active”继续当实时事实。本轮不连接后台、不修改DNS。

## 6. 首页程序化 X 动画（09-22）

36. **CollectUI曲线/海浪参考，顶部白到黑、黑占多数；希望符合集团气质的程序动画。** 参考链接保留：https://collectui.com/designs/framer-ui-design-inspiration/7cc52ed7-2b44-4573-86b9-92290655b629 。这是创意参考，不是复制该网页资源的授权。
37. **确认图片1的X造型+图片2细腻光感，排除粗糙AI纹理；比较Photoshop/Blender/代码后授权实现。** `235ffa9` 原生WebGL+SVG降级，不新增视频服务或框架。
38. **X以画面中心锁定，严格参照比例/明暗虚实，给噪点强度与粗细控制。** `cc062f0`；2876:1580比例，静态噪点；分析式重建不声称逐像素复刻。
39. **不同设备/拖动窗口不能拉伸；缩小只裁切，手机独立高度；保留已调噪点。** `6d2ad5f`，12%/0.9px；按构图高度固定比例，不按视窗宽度压扁。
40. **先讨论动态；图标式播放暂停；增加可隐藏本地工具箱、手机/桌面切换、随可见区域显示选项。** `5429a70`；只本地dev开放，工具箱里真实iframe视窗，访客不见工具控件。
41. **手机X先定360；先提出两方案，再选择第一个：白场形成，且小X上下大区域也必须是全画面动画。** `a8f3d95`；首屏容器与X构图分开，场景覆盖全viewport。
42. **现动画太静，要求桌面X高度、白场/停留等每段可调；先出方案再实施180°旋转。** `406e803`；从慢到快并柔和停稳，旋转前后停留；返回白场沿四臂扩张。工具箱扩至380px，折叠分组/时间轴/暂停/重播，暂定28s。
43. **同轮修页脚语言：只显示当前FR或EN，点击切另一语言，缩小字号并对齐。** `406e803`；与法律链接同字号/基线，手机菜单保留双语言入口；构建验证所有配对页。
44. **当前截图确认：桌面X650、手机X390；时长0/5/0/4/1/5共15秒；要求从启动到现在每步有日志、README可快速交接、项目Map和注意事项。** 本轮改配置、实际网站组件和工具箱旧设置迁移，新增本日志体系/Map/根AGENTS引导，修正过时状态。具体数值见 [Hero参数历史](hero-parameters.md)。
45. **最新补充：持续流光横跨旋转前停留、旋转180°、旋转后停留；形成和退场不用这层流光。** 本轮新增一条连续流光时钟：宽柔光沿对角线缓慢改变X内部明暗，细高光沿另一侧弧边更快游走；跨三阶段不断开、不重置。只在完整X阶段平滑进入/退出，进入白场转换时为零；减弱动态模式关闭。当前旋转前停留为0，因此默认流光覆盖4秒旋转+1秒旋转后停留；以后调长前停留也自动连续覆盖。

## 当前未完成 / 不应误认已完成

- 全面正式上线（`dev → main`）尚未执行，生产仍按占位分支处理。
- 远端部署完成、DNS/TLS/重定向的当前健康状态须发布时复核；本地构建通过不是证据。
- 表单真实访客信息与实际收件的完整验收、隐私政策法律依据与无勾选表单一致性待确认。
- 部分品牌/团队/媒体图库素材、图注及品牌英文语气需最终审核。
- Search Console验证/提交sitemap、生产索引开启、依赖安全升级、真实手机性能/无障碍检查属于上线工作。
- CMS、摄影轮播、Resend自定义发信、Affinity原生画板一键导入均未作为当前完成能力交付。
- Graphify原地图在L’Entropiste Shopify项目；本次XIONS Map是源码核对的独立Mermaid图。

## 本轮验证和发布记录

- 验证目标：15s相位边界、三阶段连续流光且转换期为零、390/650固定比例裁切、旧工具箱设置迁移、法英构建与文档链接。
- 已通过：npm run build（38个HTML内部链接、34个双语页面）、Astro检查（48文件，0错误/警告）；配置断言覆盖零停留、15秒循环、旋转180°、三阶段时钟连续、形成/退场/减弱动态流光为零；12份交接文档的相对链接有效。
- 浏览器确认：迁移后首屏最低高度仍796px，X为390px；桌面为650px；试设4秒前停留比较5.7s和7.6s时X角度不变而光层移动，随后恢复批准的0秒前停留/15秒循环。WebGL已就绪；这不是实体手机GPU性能验收。
- 附带发现：本地Astro的 `/fr` 无尾斜杠返回404，而 `/fr/` 正常；现有部分法文链接省略尾斜杠。已登记上线前路由一致性待办，本轮未扩展修改所有链接。
- 本轮按用户“本地先修改”处理，未推送或修改main；Git实时状态可判断后续是否提交。
- 具体技术机制：[Hero技术说明](../home-hero-animation.md)。当前交接入口：[日志README](README.md)。
