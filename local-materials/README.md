# Local materials

Keep company source documents, editable office files, raw copy and other non-website materials here. Git ignores everything in this directory except this instruction file.

Recommended local folders:

- `business/` — presentations, plans and company documents
- `content/` — raw RTF, Word and copy-editing source files

Only reviewed, web-ready content should move into `content/`, `src/` or `public/`.
