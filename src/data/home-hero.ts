export type HeroSlide = { src: string; alt: string };

// Add approved landscape campaign images here after placing them in
// public/images/home/. The first image is loaded eagerly; following slides lazy-load.
export const heroSlides: HeroSlide[] = [];
